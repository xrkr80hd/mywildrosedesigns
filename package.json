{
  "name": "wild-rose-site-deploy",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "dependencies": {
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "scripts": {
    "build": "npm --prefix web ci && npm --prefix web run build",
    "dev": "npm --prefix web run dev",
    "start": "npm --prefix web run start",
    "lint": "npm --prefix web run lint"
  }
}
